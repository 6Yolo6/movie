# ============================================================
# Gying Movie - Production Configuration
# ============================================================
# Usage:
#   java -jar movie-backend.jar --spring.profiles.active=prod
#
# All sensitive values use environment variables.
# Set them in .env file or system environment before starting.
# ============================================================

server:
  port: 8880
  # Graceful shutdown for zero-downtime deployments
  shutdown: graceful

spring:
  application:
    name: gying-movie
  lifecycle:
    timeout-per-shutdown-phase: 30s

  datasource:
    url: jdbc:mysql://${DB_HOST:localhost}:${DB_PORT:3306}/${DB_NAME:gying}?useSSL=true&serverTimezone=Asia/Shanghai&allowPublicKeyRetrieval=false&useUnicode=true&characterEncoding=UTF-8
    username: ${DB_USER}
    password: ${DB_PASSWORD}
    driver-class-name: com.mysql.cj.jdbc.Driver
    hikari:
      maximum-pool-size: 20
      minimum-idle: 5
      idle-timeout: 300000
      connection-timeout: 30000
      max-lifetime: 1800000

  data:
    redis:
      host: ${REDIS_HOST:localhost}
      port: ${REDIS_PORT:6379}
      password: ${REDIS_PASSWORD:}
      timeout: 5000ms

minio:
  url-prefix: ${MINIO_URL_PREFIX}

app:
  internal-token: ${APP_INTERNAL_TOKEN}
  cors:
    allowed-origin: ${CORS_ALLOWED_ORIGIN}

resource-hub:
  enabled: ${RESOURCE_HUB_ENABLED:false}
  auto-approve: ${RESOURCE_HUB_AUTO_APPROVE:true}
  tmdb:
    api-key: ${TMDB_API_KEY:}
  pansou:
    base-url: ${RESOURCE_HUB_PANSOU_BASE_URL:http://pansou:8888}
    token: ${RESOURCE_HUB_PANSOU_TOKEN:}
  quark:
    base-url: ${QUARK_AUTO_SAVE_BASE_URL:http://quark-auto-save:5005}
    token: ${QUARK_AUTO_SAVE_TOKEN:}
    save-path: ${QUARK_AUTO_SAVE_PATH:/GYing Resource Hub}
    pattern: ${QUARK_AUTO_SAVE_PATTERN:(.*)\.(mp4|mkv|avi|mov|flv|wmv|webm|m4v|ts)}
    replace: ${QUARK_AUTO_SAVE_REPLACE:}
    run-immediately: ${QUARK_AUTO_SAVE_RUN_IMMEDIATELY:true}
  worker:
    enabled: ${RESOURCE_HUB_WORKER_ENABLED:false}
    fixed-delay-ms: ${RESOURCE_HUB_WORKER_FIXED_DELAY_MS:60000}
    task-limit: ${RESOURCE_HUB_WORKER_TASK_LIMIT:5}
    quark-limit: ${RESOURCE_HUB_WORKER_QUARK_LIMIT:5}
    publish-limit: ${RESOURCE_HUB_WORKER_PUBLISH_LIMIT:20}

jwt:
  secret: ${JWT_SECRET}
  expiration-ms: ${JWT_EXPIRATION_MS:86400000}

mybatis-plus:
  configuration:
    map-underscore-to-camel-case: true
  global-config:
    db-config:
      logic-delete-field: deleted
      logic-delete-value: 1
      logic-not-delete-value: 0

logging:
  level:
    root: WARN
    com.gying.movie: INFO
  file:
    name: /app/logs/gying-movie.log
  logback:
    rollingpolicy:
      max-file-size: 10MB
      max-history: 7
