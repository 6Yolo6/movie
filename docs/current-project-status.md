# 当前项目状态

更新时间：2026-08-21

本文件只记录当前能力、长期约束和仍需处理的工作。历史验证样例、一次性资源 ID、临时调试过程不在此保留；具体接口和部署参数分别以 `docs/api.md`、`docs/deployment.md` 为准。

## 当前目标

- 维护电影、剧集、动漫片库及用户提交资源。
- 通过 TMDB、PanSou/Panso API、quark-auto-save 和 GYING 自动补全元数据与网盘资源。
- 支持 QQ 群资源查询、两个独立腾讯频道账号和新浪微博资源帖，并保留可审计记录。
- 所有清理和修复保持幂等、可追踪，核心影片和资源只做软删除。

## 运行架构

- 对外入口：nginx `80/443`，反向代理 Next.js 前端和 Spring Boot 后端。
- 核心服务：`frontend`、`backend`、`gying-source`。
- 数据依赖：MySQL、Redis、MinIO；资源搜索和转存连接现有 PanSou、quark-auto-save 服务。
- QQ 集成：NapCat/官方 QQBot 用于群聊；宿主机 `tencent-channel-cli` 保留原频道账号，独立 `social-publisher` 容器承载第二 QQ 账号和新浪微博。
- 生产容器和 JVM 使用 `Asia/Shanghai`。
- 微博网页会话通过 social-publisher 独立持久化浏览器会话支持管理员扫码更新，会话文件只保存在运行时卷。

## 已具备能力

### 片库与治理

- 片库分页、分类筛选、详情、收藏热门、评论留言和站内通知。
- 用户可提交、编辑和软删除网盘/磁力/种子/在线播放资源。
- 管理端支持资源审核、批量审核、举报处理、失效标记、评论和用户管理。
- 管理员编辑资源时，URL 未变化可正常保存其他字段；只有改成另一条活动资源的 URL 时才提示重复。
- `movie_metadata` 与 `resource_link` 使用 `deleted_at` 记录软删除；历史关系不物理删除。
- 重复 TMDB 影片和标题污染资源提供 dry-run 后清理，优先保留现有片库 canonical 记录。

### 影视资源中心

- TMDB 热门/高分采集会匹配现有影片，避免覆盖站内收藏热度或创建重复剧集。
- TMDB 自动采集按配置间隔每次轮询一个来源，多个来源分散到全天执行；活动任务存在时不会重复入队。
- 资源中心看板显示 TMDB 影片总数、近 24 小时同步/新增/失败任务、资源发现和入库统计，并显示最近与下次采集时间。
- 资源搜索同时合并本地 PanSou 与外部 Panso API，按 URL 去重。
- 自动资源必须先转存到自有夸克或迅雷目录并创建自有分享，之后才能写入 `resource_link`。
- PanSou 与 GYING 已识别迅雷候选并创建独立 `xunlei_transfer_task`；迅雷 runner 优先于历史夸克积压执行，支持受 internal token 保护的指定任务重试。
- PanSou 的迅雷链接健康结果只作为提示，不再因 `bad` 直接忽略候选；链接是否可用由官方迅雷转存 API 最终判定。夸克候选仍保留发现阶段的失效拦截。
- 迅雷任务从失败恢复为成功时会显式清空旧 `last_error`，避免管理页同时显示成功状态和过期错误。
- 迅雷转存与公开分享已完成真实链路验证；生产 `XUNLEI_SHARE_ENABLED=true`，没有自有分享时发布器仍禁止回退写入第三方迅雷源链接。
- 迅雷 restore 现在递归筛选源分享中的视频文件 ID，只转存视频，不再连带广告图片、文档或整个顶层目录；自动失败任务最多尝试 3 次，手工重试可显式重新执行。
- “发现结果”的重试分享与发布按 provider 分流：迅雷记录调用迅雷 runner，夸克记录调用 quark runner，避免迅雷链接误送至 quark-auto-save。
- 迅雷分享文件解析兼容真实 API 的 `file_id`/`fid`、`filename`、`items`、camelCase MIME/扩展名等字段，并可从文件名后缀识别视频，避免链接内有视频却误报“contains no video files”。
- `AUTO` 发现即使 GYING 已返回夸克候选，也会继续补充搜索迅雷候选；QQ 资源列表会同时读取系统自有夸克、迅雷和待选择的迅雷发现结果，不再因夸克先命中而跳过迅雷。
- 发现结果支持标题/影片 ID/链接筛选、时间排序、可点击来源/自有分享和复选批量操作。
- 空目录或分享失败先重跑原转存，仍失败时重新搜索、转存和发布。
- “缺资源影片”按实际可用 `DISK` 链接口径检查，可选择 GYING 或 PanSou 补全。
- 缺资源补全会按候选逐条转存，带年份失败后改用纯片名继续搜索；只有活动资源真正写入后才返回成功，GYING 会优先复用来源身份并自动扫描目录定位，仍失败时回退 PanSou。
- 缺资源列表支持跨页勾选并批量从 GYING 或 PanSou 补全，每批最多 20 部；后端按影片记录逐条成功/失败结果，超限时明确提示而不静默截断。
- 发现状态可用 dry-run 预览后校准历史标题误判、旧任务冲突和发现/转存状态不一致；真正不相关标题及季号冲突仍保持忽略。
- 重试分享会为没有任务或任务已取消的发现重建转存任务；没有生成自有分享及活动正常网盘资源时明确返回失败，不再以“跳过”冒充完成。
- TMDB 剧集记录会补全系列名和目标季；转存多季合集时先递归定位明确的目标季目录并收窄分享 `fid`，再写入独立季目录，找不到对应季时改试下一候选。
- 第 1 季夸克来源在没有显式季目录时，会在无冲突季标记且根目录直接含视频或仅含一个有视频的无标记目录时使用根目录/该目录；多季冲突仍拒绝。
- 电影合集会按中文名、英文名和年份递归定位明确的单片目录；命中后只递归转存该目录，不把整套合集写入目标影片目录。
- TMDB 同步失败不会再显示为采集成功；网络线路可通过 `TMDB_API_BASE_URL` 调整。
- TMDB 客户端按最小请求间隔访问，并对 429、5xx 和连接超时做有限重试；当前生产失败样例主要是外部网络连接超时，不是数据库认证失败。
- 自动资源发现使用 `AUTO` 路由时先做 GYING 精准身份与资源搜索，只有 GYING 无严格结果或转存失败才进入 PanSou；GYING 转存失败会在 24 小时内去重排入 PanSou 兜底任务。
- PanSou 明显无关的上游结果不再逐条写入“已忽略”，只保留有效链接的审计记录；历史 `Resource title does not match movie title` 记录保留，不执行物理删除。

### GYING 数据源

- `gying-source` 支持最近更新、单片采集、预告片补全、本人已发布资源检查和批量修复。
- 管理页支持按电影、剧集、动漫浏览 GYING 高分/最新/热度目录，并可按页批量确保资源。
- `movie_source_identity` 以来源、类型、外部 ID 和季号连接 TMDB、GYING 与本地 canonical 影片；自动关联只接受严格片名、类型及年份/季号等元数据锚点。
- 剧集可一键补齐其余季；历史 TMDB 影片缺少 GYING 身份时，先扫描片库目录严格定位并建立身份，再处理各季资源。
- 单片与批量缺海报修复优先重新请求 TMDB 海报，TMDB 不可用时再按已确认或严格发现的 GYING 身份回补。
- GYING 影片 ID 与本地 canonical 影片 ID 分离，支持资源写回已有 `tmdb_*` 或片库记录。
- 本地无资源时可验证并尝试最多 5 个 GYING 夸克候选，成功转存后发布自有链接。
- 失效资源优先复用 `saved_path` 重分享，失败时改用同片其他候选，并保留原 GYING `panlist.id`。
- 共享站点 Session 有锁保护，并处理 PoW、登录过期和 AJAX JSON/HTML 响应差异。
- GYING 搜索参数已按站点分类约定映射：`0` 为全部、`1` 为电影、`2` 为剧集、`3` 为动漫；普通候选使用适中模式，自动身份绑定使用精准模式，并限制模式值为 `1..3`。
- GYING 定时目录采集成功后会为本地影片排入 `AUTO` 资源发现任务，沿用 GYING 优先、PanSou 兜底、夸克转存、自有分享和正式入库流程；已有可用资源或冷却期内任务会跳过。
- 管理页可切换当前 GYING 用户名、密码、Cookie 和发布者显示名；凭据不回显、不入库，容器重启后恢复环境变量默认值。
- GYING 网盘发布使用固定 `POST /res/pan/add`，通过 `binds[0][dir]` 与 `binds[0][id]` 绑定影片；旧的路径参数形式会被上游返回 404。
- GYING 管理页“最近更新”支持勾选后批量确保资源并发布；“已发布资源健康”支持输入一个或多个 GYING `panlist.id` 定向修复，最多 100 个，未找到和 `UNCLEAR` 资源不会被修改。

### QQ 自动化

- QQ 群查询先精确匹配本地影片和已发布资源，缺失时才进入 TMDB、搜索、转存、分享和发布链路；每次资源结果只返回已验证的系统自有夸克或迅雷分享。
- QQ 群查询采用两阶段候选：先返回影片候选，用户选定影片后返回资源名称/画质候选；只有回复单个资源序号才创建并执行对应的夸克或迅雷转存任务。可用“夸克”或“迅雷”筛选，`夸克10` 等数量指令只展示候选，不会批量创建重复任务。
- QQ 即时发现使用延迟转存；搜索阶段不创建夸克/迅雷任务，选定资源后只执行对应提供方。搜索开始立即回复“正在搜索资源，请稍后...”，多次失败、无资源、无视频文件或违规/平台拦截均返回明确提示。
- 带季数搜索会严格命中对应季；同样健康且覆盖整季/全集时，优先管理员或用户明确投稿的自我分享，再使用自动转存链接。
- 群内直回消息会 @ 发起用户；未知或空 AT 消息返回使用帮助。OpenClaw 补丁通过 `qrcode` 为回复中的夸克分享生成二维码图片。
- `/admin/automation` 管理搜索日志、频道发帖日志和自动化配置。
- QQ 搜索看板统计搜索总量、近 24 小时搜索、成功率、无结果和需处理记录。
- 资源中心手动发 QQ 通过受内部令牌保护的宿主机桥接立即执行；失败时保留 `PENDING` 供定时任务处理。
- 频道帖子模板支持 `{{title}}`、`{{year}}`、`{{type}}`、`{{link}}`、`{{intro}}`；自动候选按站内热度、TMDB 热度和资源录入时间排序，同一影片只取一条。
- 频道帖子使用 UTF-8 内容文件、可点击资源链接和影片海报，并用全局互斥锁避免并发发帖。
- 本地和 TMDB 都无法精确命中时，机器人优先调用 GYING 搜索并展示带来源的影片候选；用户回复影片序号后只导入元数据，再展示资源名称/画质候选。用户确认资源后才按候选来源身份转存，失败再回退本地 PanSou 与外部 Panso API。

### 多平台发布

- `/admin/automation` 增加“多平台发布”页签，维护多个 QQ 账号频道目标和 1 个新浪微博目标；QQ 可在页面新增账号、生成二维码扫码授权和移除账号。
- 发布目标可在管理页单独删除；删除配置前会终止该目标仍在等待的任务，已发布和失败历史日志继续保留，已删除目标的历史记录不再提供无效重试。
- 支持单目标一键发下一条、全部目标批量发布、每日定时、每次条数、间隔和正文模板。
- 页面展示分页发布记录，可按平台和状态筛选，查看外部帖子地址、失败原因并重试失败记录。
- 候选按站内热度、TMDB 热度和资源录入时间排序；同一目标按影片去重，不因资源链接更新重复发同片。
- 每个 QQ 发布账号使用独立凭据目录并保存在专用容器卷；微博使用独立发布容器的网页会话环境变量，不覆盖原 QQ 机器人或宿主机频道账号。
- 发布日志记录 `PENDING`、`POSTED`、`FAILED`、外部帖子地址和失败原因；两个平台未授权时只影响对应独立目标。
- 2026-08-05 已通过独立第二 QQ 账号真实发布并写回外部帖子地址；2026-08-06 已用微博网页会话手动发布测试微博，项目发布器已切换为网页表单请求，不再依赖官方 CLI 套餐。

## 配置与安全

- 密钥、Cookie、密码、Token 只放 `.env`、外部服务配置或进程内存，不提交到 Git。
- Resource Hub 和 QQ 自动化的非敏感运行参数保存在 `sys_config`。
- Resource Hub 管理页可在迅雷 Authorization 或 CAPTCHA Token 过期后直接更新当前后端进程内的凭据；接口只返回是否已配置，不回显或入库敏感值，容器重启后仍以 `.env` 为默认来源。
- “系统设置”按键名前缀展示、搜索和编辑 `sys_config` 全部现有配置，支持布尔、数字、单行和多行值；新增数据库配置不需要再修改前端固定键名列表。
- 数据库新增表和字段默认使用中文 `COMMENT`。
- 外部结果必须通过标题相关性、链接状态和重复检查后才能发布。
- 不重复启动已有 Redis、PanSou、quark-auto-save 或 MinIO 容器；生产 compose 默认连接现有依赖。

## 仍需处理

- 用一条计划保留的真实资源验证已部署的 GYING 发布接口修复；容器健康、只读候选和请求契约已验证，但尚未执行真实发布。
- 将当前 OpenClaw QQBot 的本机 `node_modules` 补丁升级为正式私有插件或上游配置。
- 在管理员权限下重新启用每日腾讯频道自动发帖计划任务；手动立即发帖不受影响。
- 配置新的 `WEIBO_WEB_COOKIE` 与 `WEIBO_WEB_FINGERPRINT`，完成项目内单条真实发布验收后再开启微博目标自动发布。
- 持续 dry-run 清理早期 `qq_<hash>` 占位影片及重复 TMDB 记录，执行时迁移关系后软删除。
- 用计划保留的真实影片再做一次 TMDB -> 搜索 -> 转存 -> 分享 -> 入库 -> QQ 回发全链路验收。
- 豆瓣评分暂无可靠官方 API，不作为稳定生产能力。

## 长期不变量

- `movie_metadata` 是影视主表，`resource_link` 是可用资源链接表。
- `movie_metadata.popularity` 只表示站内收藏热度；TMDB 热度使用独立字段。
- 自动采集只在拥有可用自有分享后发布，不能把第三方夸克或迅雷源链接直接写成正式资源。
- 本地模糊匹配只能提供候选，不能直接触发转存或回复。
- 失效修复原位更新原资源；临时新资源成功后应归并，避免同片多条活动自有分享。
- 删除核心数据、重复清理和标题匹配不确定时先 dry-run，不执行物理删除。

## 验收

2026-08-21 修复迅雷分享内有视频却误判为空，以及 GYING 夸克先命中后跳过迅雷搜索的问题：文件解析扩展到真实迅雷 API 的多种字段和文件名后缀，并递归读取分享分页和 parent_id 子目录；`AUTO` 发现会补充迅雷候选，QQ 可同时展示库内自有迅雷与待转存迅雷候选。GYING 数据源抓取、入库、发布和手动资源 ID 修复按 provider 分流，已发布链接失效时会重分享或重转存，发布回写保留迅雷 provider。后端完整测试通过，生产 backend 已重建；`/api/qq-bot/health`、片库 API、nginx 首页验证通过，4496 的真实定向重试仍待管理员在页面触发验收。

2026-08-20 已完成 QQ 机器人两阶段影片/资源候选选择、延迟转存和失败可见提示：搜索先回复“正在搜索资源，请稍后...”，用户确认单个资源后才执行对应夸克或迅雷任务；`夸克10` 等数量指令不再批量转存。同期完成迅雷视频文件过滤、失败任务自动重试上限、发现结果按 provider 重试，以及 QQ 机器人自有链接容错：GYING/Resource Hub 自有夸克与迅雷链接不再因 PanSou `bad` 被直接隐藏；后端定向测试与完整测试通过。

2026-08-19 已修复迅雷候选被 PanSou 健康检查误判为 `bad` 后直接忽略的问题：迅雷候选改由官方转存 API 做最终校验，夸克早期失效拦截保持不变；后端完整测试通过。玩具总动员 5 的夸克任务已排除违规文件后创建自有分享并正式入库，夸克与迅雷活动资源均存在。更新 Authorization 后定向修复最近 11 个受影响影片：10 个通过官方 API 完成转存、自有分享和正式入库，1 个由官方 API 明确判定为分享内无文件；未再回退第三方链接。

2026-08-18 已完成 QQ 机器人自有网盘过滤与双网盘采集链路：默认及指定资源查询只接受 `RESOURCE_HUB`、`GYING_PUBLISHED` 或已核验转存任务产生的夸克/迅雷分享；用户指定百度或其他网盘时不执行第三方搜索。该链路已由本次两阶段候选改为选定资源后定向转存，未部署真实环境。
2026-08-18 已新增 Resource Hub 管理页迅雷运行时凭据表单：管理员可更新 Authorization 或 CAPTCHA Token，凭据只写入当前后端进程内存，响应仅返回配置状态，重启后从 `.env` 恢复。提交 `e4f98c3` 已推送；backend/frontend 镜像已重建并启动，backend 启动日志正常，nginx 首页和 `/api/movies/list` 均返回 200；本次运行时镜像分别为 `sha256:720b3ad7…`（backend）和 `sha256:79bac0e8…`（frontend）。后端 `mvn clean test`、前端 lint/build、Compose 配置校验和运维 readiness 检查通过。

2026-08-17 已部署夸克第 1 季根目录选择、Resource Hub 定向转存/发布 internal 接口及上一轮迅雷错误字段清理。发现 `4176`、`4177` 均完成转存、自有分享和正式入库，状态为 `SAVED`，活动夸克资源为 `ACTIVE/NORMAL`；同片既有管理员资源保留，未删除历史记录。生产 backend 镜像已重建，片库 API 返回 200；后端完整测试、采集器编译、Compose 配置和运维就绪检查通过。迅雷任务 `12` 再次定向执行返回 `skipped=1`，没有重复转存或分享；发现 `4044` 与资源 `2003` 的遗留错误字段已清空。

2026-08-14 已部署迅雷配置绑定、发布门禁、优先调度和定向重试：修正 `resource-hub.xunlei` YAML 层级，避免历史夸克失败积压饿死迅雷队列；没有自有分享时明确跳过正式入库。更新 Authorization 后，真实 GYING 迅雷候选已成功转存并创建自有分享，任务 `12` 已验证幂等重试；生产保持发布门禁，不把第三方迅雷源链接写入正式资源。

2026-08-13 已完成并部署 GYING 管理页批量操作：提交 `fff2028` 已重建 backend、frontend、gying-source、nginx；前端支持最近更新勾选批量确保，健康页支持单条或最多 100 个 `panlist.id` 定向修复，接口分别为 `/recent/ensure` 和 `/published-resources/repair-by-ids`。backend 完整测试、前端 lint/生产构建、Compose 配置、容器状态、source 健康、backend API、nginx 首页和管理页静态渲染均通过；未执行真实修复任务。旧应用镜像已在部署前记录，可按服务回滚。

2026-08-12 已定位 GYING 发布 502 为上游接口契约变化：发布入口从路径绑定改为固定 `/res/pan/add`，影片类型和 ID 改由 `binds[0]` 表单字段传递。提交 `39c6213` 已部署到生产 `gying-source` 镜像 `sha256:359aebd…ae5da`；容器健康、账号配置状态、只读最近更新、backend 到 source、backend API 和 nginx 首页均验证通过，容器内源码确认使用新契约且重建后无异常日志。旧镜像 `sha256:fc15fed…2434b` 保留为应用回滚基线；本次未改数据库或卷，也未执行真实发布。

2026-08-10 已完成多平台发布目标删除和全量 `sys_config` 管理页改造；删除目标会保留 `social_post_log` 并终止待发布记录。后端完整测试、前端 lint/生产构建和 `git diff --check` 通过；生产 `backend`、`frontend` 容器已重建，后端片库接口及 nginx 下 `/admin/settings`、`/admin/automation` 均返回 200。

2026-08-10 已完成 GYING 搜索分类与模式修复、GYING 优先资源发现和 PanSou 失败兜底回归测试；`python -m unittest crawler.test_gying_crawler` 通过（3 项），`python -m py_compile crawler/gying_crawler.py` 通过，后端 GYING/夸克转存目标测试通过，`git diff --check` 通过。生产数据库只读统计确认历史有 2112 条 PanSou 标题噪声忽略记录，未执行数据删除；新任务不再为明显无关结果逐条落库。

2026-08-03 已完成 TMDB 全天滚动采集、采集/QQ 搜索看板、频道模板年份/类型和热度候选排序；后端完整测试、前端 lint/生产构建和生产 Compose 配置检查通过，lint 保留 4 条既有 warning，无 error。

2026-07-21 已确认 `movie_source_identity` 迁移落库；后端完整测试、采集器编译、前端 lint/生产构建和生产 Compose 构建通过。lint 保留 4 条既有 warning，无 error；`backend`、`frontend`、`gying-source`、`nginx` 已使用新镜像运行。

2026-07-22 已完成 Resource Hub 历史状态校准：17 条标题误判和 304 条旧任务冲突恢复为可重试，163 条发现/转存状态不一致同步为失败，847 条真正不相关标题继续保持忽略；校准未触发真实转存。缺资源页确认 121 部影片可跨页勾选并启用 GYING/PanSou 批量补全。

2026-07-23 已完成电影季号污染迁移：229 部电影季号置空，39 条电影来源身份统一为季号 `0`，2 条唯一键冲突的历史身份保留并标记为拒绝，活动电影季号污染归零。`tmdb_movie_1275779` 通过精确搜索绑定 GYING `mv/0pEK`，`tmdb_movie_278` 复用 `mv/mXr4`；两部电影均已真实转存、创建自有分享、发布到 GYING 并更新为 `AVAILABLE`，新电影任务不再请求“第 1 季”目录，历史失败任务继续保留审计。

2026-07-31 已完成夸克合集电影目录修复：任务 `766` 从“人生七年 1-9 部合集”精确定位 `人生七年8 56 Up (2012)` 子目录，提交链接带目标 `fid` 且 `update_subdir=.*`。自有分享 `e9cbb857c629` 只包含 `56 Up` 的 3 个 2012 年视频文件，发现记录 `2515` 更新为 `SAVED`，资源 `1887` 更新为 `ACTIVE/NORMAL`。后端完整测试、采集器编译和单测、前端 lint/生产构建、生产 Compose 配置检查通过；lint 保留 4 条既有 warning，无 error。

```powershell
cd backend
mvn test

cd ../frontend
npm run lint
npm run build

cd ..
docker compose -f docker-compose.prod.yml config --quiet
```
